name: Jira Plugin - Ubuntu 24.04

on:
  workflow_dispatch:

jobs:
  test-jira:
    runs-on: ubuntu-24.04
    
    steps:
      - name: Check Ubuntu version
        run: |
          echo "🎯 Quick test on Ubuntu 24.04"
          lsb_release -a
