@inherits LayoutComponentBase

<div class="page">
    <div class="sidebar">
        <NavMenu />
    </div>

    <main>
        <div class="top-row px-4" style="display: flex !important; justify-content: space-between !important; direction: rtl !important; width: 100% !important; padding: 0.75rem 1.5rem !important;">
            <div class="top-row-right" style="display: flex !important; align-items: center !important; gap: 1.5rem !important;">
                <div class="app-title" style="display: flex !important; align-items: center !important; gap: 0.5rem !important;">
                    <span class="app-icon">🏭</span>
                    <span style="font-weight: 700; color: #1e3a8a; white-space: nowrap;">سیستم Paperless Manufacturing</span>
                </div>
                <div class="top-nav-links" style="display: flex !important; align-items: center !important; gap: 0.75rem !important;">
                    <a class="top-nav-link" href="">🏠 داشبورد</a>
                    <a class="top-nav-link" href="inspection/in-process">🔍 فرم بازرسی</a>
                    <a class="top-nav-link highlight-link" href="reports">📊 گزارش‌ها و خروجی اکسل</a>
                </div>
            </div>
            <div class="user-info" style="white-space: nowrap; color: #64748b; font-size: 0.85rem;">
                <span>کروز - واحد کنترل کیفیت</span>
            </div>
        </div>

        <article class="content px-4">
            @Body
        </article>

        <footer class="app-footer">
            <span>Atlassian Paperless Forms Software (v1.0.0#sha1:crouse:@_nodeId) · <a href="/paperless/">About</a></span>
        </footer>
    </main>
</div>

<div id="blazor-error-ui">
    خطایی رخ داد. لطفاً صفحه را مجدداً بارگذاری کنید.
    <a href="." class="reload">بارگذاری مجدد</a>
    <button class="dismiss">✕</button>
</div>

@code {
    private string _nodeId = string.Empty;

    protected override void OnInitialized()
    {
        var envNode = Environment.GetEnvironmentVariable("NODE_ID");
        if (!string.IsNullOrEmpty(envNode))
        {
            _nodeId = envNode;
        }
        else
        {
            var host = System.Net.Dns.GetHostName();
            _nodeId = host.Contains("42") || host.Contains("node1") ? "node1" : (host.Contains("43") || host.Contains("node2") ? "node2" : host);
        }
    }
}
